<%= form_with(model: tag) do |form| %>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div> <!-- name container -->

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>